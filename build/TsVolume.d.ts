export declare class TsVolume {
    private _canvas;
    private _slider;
    constructor(canvas: HTMLCanvasElement);
    private _addEventListener;
    render(ctx: CanvasRenderingContext2D): void;
}
